import styled from "styled-components";

export const RightContent = styled.div`

    grid-area: right;
    display: flex;
    width: 40rem;
    height: 40rem;
    
    justify-content: center;
    align-items: center;

    position: relative;

`